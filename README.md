The links for Act1 and Miniproject.php follow the same pattern in opening PHP files because both are stored inside the same project folder under the XAMPP htdocs directory.
In XAMPP, the standard format for accessing PHP files is http://localhost/foldername/filename.php. 
That is why both activities use the same URL pattern, only the PHP filename changes depending on the file being opened.
